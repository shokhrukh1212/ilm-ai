# Diary entry — 2026-05-30

## What was done

### Phase 4 — Quiz generator + grader (completed)
- Added `0004_quiz.sql` migration: `quiz_sessions`, `quiz_questions`, `quiz_answers` with per-user RLS (nested ownership for questions/answers) and indexes.
- `quiz_gen` agent (GPT-4o) with Pydantic-AI typed output (`QuizSet`/`QuizQuestion`), validators enforcing exactly-4-option MCQs and `correct_answer ∈ options`, and `retries=2` so malformed generations are re-requested.
- `quiz_explainer` agent (Claude Sonnet 4-6) returning `{is_correct, feedback}` — grades open-ended answers and writes warm, source-referencing feedback.
- `quiz_sources` service: even-sampling of child chunks across the whole material (capped at 40), labeled by real `chunks.id` so generated `source_chunk_ids` resolve back to citations.
- `quiz` router: `POST /generate` (15s timeout), `GET /{id}` (take view, no answers), `POST /{id}/answer` (deterministic MCQ grading + AI feedback), `POST /{id}/finish` (persists score), `GET /{id}/results` (full review with citations).
- Frontend: `/quiz/new` (config), `/quiz/[id]` (one-question-at-a-time + progress bar + immediate feedback), `/quiz/[id]/results` (score + per-question review with citation chips → PDF sheet). Added `Mashq` nav entry, dashboard card, and a library-detail "Mendan so'rab ko'r" CTA. Added a `textarea` shadcn component for open answers.
- Tests: migration, generator validators/prompt/sampling, explainer turn/sanitization, router validation + ownership/state guards.

### Phase 5 — Gap detection + learning plan (completed)
- Added `0005_gaps_plans.sql`: `knowledge_gaps` + `learning_plans` with per-user RLS and indexes.
- `gap_detect` agent (Claude Sonnet 4-6, exact §5 prompt as system prompt, `GapSet` typed output, `retries=2`) and `planner` agent (Claude Sonnet 4-6, exact §5 prompt formatted per request, `LearningPlan` typed output).
- `services/gap_detection.py`: pulls last-90-day quiz history, runs the agent, and upserts gaps by normalized topic in a transaction (closing gaps that no longer appear); derives each gap's material from its dominant evidence material.
- Trigger: `quiz.finish` enqueues `run_gap_detection` via FastAPI `BackgroundTasks` (async; finish response unchanged).
- Routers: `GET /gaps`, `POST /gaps/detect`; `POST /plan/generate` (default target = today+7), `GET /plan`, `POST /plan/{id}/task` (toggle `done` via read-modify-write of the plan jsonb).
- Frontend: `/gaps` (severity-meter cards), `/plan` (dated task cards, re-generate config, per-task done toggle), `GapCard`/`PlanDay` components, dashboard cards, and a "7 kunlik reja tuzish" CTA on quiz results.
- Tests: migration, gap_detect severity bounds + prompt, planner models + prompt formatting, router/service helpers (request validation, plan jsonb parsing, dominant-material, normalize).

---

## Problems encountered — Phase 4

1. **Explainer prompt has per-answer placeholders** — The blueprint Quiz Explainer prompt embeds `{lang}/{prompt}/{correct}/{user_answer}`, so it can't be a static system prompt (literal braces would leak).

2. **Quiz generation has no search query** — Unlike chat, there's no user query to retrieve against, so the generator needs representative source coverage.

## Solutions — Phase 4

1. Kept the exact blueprint text as a template formatted per request and delivered as the run input (same pattern as the generator), with the agent a reusable singleton typed to `Explanation`.

2. Even-sample child chunks across the material by stored order (stride = ceil(total/cap)), labeling each by real chunk id so questions cite real sources.

---

## Problems encountered — Phase 5

1. **No topic_tags on questions** — The blueprint gap-detect input expects `topic_tags`, but quiz_questions stores none, so wrong answers can't be pre-clustered by topic.

2. **Gap upsert identity** — Topics are free-text agent output with no stable key, so re-running detection risks duplicate gap rows.

## Solutions — Phase 5

1. Feed the agent the question prompt text and let it infer/cluster topics (documented deviation); severity still follows the blueprint `round((wrong/total)*5)` formula.

2. Manual upsert in a transaction keyed on normalized (trimmed/lowercased) topic — update existing open gaps, insert new ones, and set `status='closed'` on any open gap not in the new detection set.

---

## Next plan
- Phase 6: Telegram bot — PTB v20 webhook, `/link` flow, daily push, inline MCQ quiz.

## Time spent
- ~4h (Phases 4 + 5)

## Demo video
- None this entry
