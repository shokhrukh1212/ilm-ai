# Diary entry — 2026-05-31

## What was done

### Phase 6 — Telegram bot (completed)
- Added `0006_telegram.sql`: `telegram_links` (unique `telegram_chat_id`, `one_time_code`, `lang`, `opt_in_daily`) with per-user RLS and indexes.
- `services/telegram_service.py` holds all bot/link DB access (service-role asyncpg, explicit `user_id` filters): one-time code create/redeem, link status, opt-in toggle, language resolve/set, today's plan tasks, day streak, opt-in user list, and in-bot quiz answer recording.
- `services/quiz_session.py`: extracted `create_quiz_session(...)` from the quiz router so the web router and the bot persist `quiz_sessions` + `quiz_questions` identically; raises `QuizSourcesEmpty`.
- `agents/quiz_gen.py`: added a Claude (`claude-sonnet-4-6`) provider variant reusing the same `QuizSet` schema/prompt, plus `resolve_provider()` which auto-falls-back to Anthropic when `OPENAI_API_KEY` is empty.
- `app/telegram/`: `strings.py` (uz-latn / uz-cyrl / ru / en copy), `handlers.py` (`/start [code]`, `/link`, `/help`, `/lang`, `/today`, `/streak`, `/quiz` + inline-MCQ callback), `bot.py` (Application factory with `AIORateLimiter`), `scheduler.py` (APScheduler daily 9am Asia/Tashkent push).
- `routers/telegram_link.py`: `POST /telegram/link/start`, `GET /telegram/link/status`, `POST /telegram/link/opt-in`.
- `main.py`: lifespan builds the PTB Application + `set_webhook` + scheduler (skipped under `IS_TEST_MODE` / no token); `POST /webhooks/telegram/{secret}` verifies the path secret and the `X-Telegram-Bot-Api-Secret-Token` header; `POST /debug/telegram/push` for manual daily-push testing.
- Frontend: `/telegram` page (link code, copy, `t.me` deep link, opt-in toggle), `lib/api.ts` client functions, Telegram as the 5th nav tab + a dashboard card, env example additions.
- Dependency: `python-telegram-bot[rate-limiter]==21.*` (adds `aiolimiter`).
- Tests: telegram migration, service pure helpers + redeem/create-code, link router, webhook secret guard, and quiz-gen provider/fallback.

### UI fix — markdown in quiz feedback (completed)
- Extracted the chat's markdown renderer into a shared `components/Markdown.tsx` (react-markdown + remark-gfm + the bold/italic/list/heading style map); `ChatStream` now imports it instead of keeping its own copy.
- Rendered the quiz AI feedback and explanations as markdown: the in-quiz feedback panel (`quiz/[id]/page.tsx`) and the results page `ai_feedback` + `rationale` (`quiz/[id]/results/page.tsx`).

---

## Problems encountered — Phase 6

1. **Only the Anthropic key is configured** — The merged Phase 4 quiz generator is hardcoded to GPT-4o, so quiz → gaps → plan couldn't run end-to-end with just `ANTHROPIC_API_KEY`, and the in-bot quiz would fail.

2. **PTB handler typing under mypy strict** — `CommandHandler`/`CallbackQueryHandler` expect a `Coroutine`-returning callback; the `@guard` wrapper was typed as returning `Awaitable`, which mypy strict rejected.

3. **Bot must not break the sandbox/tests** — Building the Application and calling `set_webhook` needs a token and network access that the sandbox lacks.

## Solutions — Phase 6

1. Added a Claude provider variant plus `resolve_provider()` that transparently switches an `"openai"` request to Anthropic when no OpenAI key is set — so generation "just works" with whichever key is present, with no config toggle.

2. Typed the `guard` alias as `Callable[..., Coroutine[Any, Any, None]]` (an `async def` returns a `Coroutine`), which satisfies the PTB handler signatures.

3. Gated the whole bot/scheduler startup behind `not IS_TEST_MODE and TELEGRAM_BOT_TOKEN`; the webhook route returns 200 as a no-op when the Application isn't built, so unit tests never touch the network. ngrok steps documented in the API README for live testing.

## Problems encountered — UI fix

1. **Quiz feedback showed raw markdown** — The grader/explainer returns markdown (`**bold**`, lists), but the quiz feedback was rendered as plain text, so users saw literal `**` asterisks instead of formatted text.

## Solutions — UI fix

1. Reused the chat's existing react-markdown setup via a shared `Markdown` component and rendered the quiz feedback/explanations through it, so bold/italic/lists display correctly (matching the chat styling).

---

### Bug fixes — quiz performance (completed)

**Issue 1: Quiz generation timing out (504)**
- `GENERATE_TIMEOUT_S` was 15 s. With no `OPENAI_API_KEY` set, `resolve_provider` silently fell back to Claude Sonnet, which takes 20–40 s to produce 10 validated JSON questions. The `asyncio.wait_for` ceiling fired first, the router returned 504, and the log showed "quiz generation timed out".
- Fix: wired in `OPENAI_API_KEY` (GPT-4o is the intended provider per the blueprint) and raised the timeout to 60 s. Added `OPENAI_API_KEY` to `.env.example` so it is no longer an invisible requirement.

**Issue 2: Quiz generation slow even with the right provider**
- The sources block fed up to 40 chunks (~12 k input tokens) into the LLM in one call. Halved `DEFAULT_MAX_CHUNKS` from 40 → 20 (~6 k tokens); `_even_sample` still spreads evenly across the full document so coverage is unchanged.

**Issue 3: Answer checking (explainer) very slow**
- Every answer — including MCQ where correctness is already decided deterministically — called Claude Sonnet 4.6 to produce 2–3 sentences of feedback. Sonnet is ~5–8× slower than Haiku for this simple task.
- Fix: switched `EXPLAINER_MODEL` from `claude-sonnet-4-6` → `claude-haiku-4-5-20251001`. The grading and short explanation are well within Haiku's capability.

---

## Phase 6 wrap-up
- Ran `0006_telegram.sql` in Supabase — `telegram_links` + per-user RLS are now live.
- Phase 6 code complete and committed to `6-telegram-bot`: `mypy` clean (59 files), 121/121 backend tests pass, web build green (15/15 routes incl. `/telegram`).
- One manual step remains before a live phone demo (the "2nd step"): create the bot with BotFather, run `ngrok http 8000`, set `IS_TEST_MODE=False` + `APP_BASE_URL`=<ngrok https url> + `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET`/`TELEGRAM_BOT_USERNAME`, then start uvicorn so the lifespan calls `setWebhook`. Steps documented in `apps/api/README.md`. Until then the bot stays dormant (gated off when `IS_TEST_MODE` or no token).

## Next plan
- Finish the Telegram live setup (BotFather + ngrok) and verify `/start`, `/link`, `/today`, `/quiz`, and the daily push on a phone.
- Phase 7: Payments — Payme + Click (PayTechUz) and Stripe; resolve PayTechUz/Pydantic-v2 compatibility first.

## Time spent
- ~3h + ~30 min bug fixes

## Demo video
- None this entry

---

# Phase 7 — Payments (Payme + Click + Lemon Squeezy)

## What was done

### Phase 7 — Payments (completed)
- `0007_billing.sql`: `subscriptions` + `payment_transactions` with per-user RLS and a unique `(provider, provider_tx_id)` index for idempotent webhook upserts.
- `services/billing.py` (pure asyncpg, service-role): `activate_subscription` (upsert sub + bump `users.tier`, default +30d period), `deactivate_subscription` (→ free), `cancel_at_period_end`, `record_transaction` (idempotent ledger upsert), `get_transaction` (Payme state machine), `get_subscription`, `user_exists`; plan→tier and plan→price (UZS) maps.
- `routers/webhooks_payme.py`: native Payme JSON-RPC 2.0 — HTTP Basic auth (`Paycom:{PAYME_KEY}`), all six methods (CheckPerform/Create/Perform/Cancel/Check/GetStatement), proper Payme error codes, tiyin→UZS conversion, state machine in `payment_transactions`.
- `routers/webhooks_click.py`: native Click Prepare/Complete — MD5 `sign_string` verified BEFORE any DB write (invalid → `error=-1`, no state change), amount validation, activate on Complete.
- `routers/webhooks_lemonsqueezy.py`: `X-Signature` HMAC-SHA256 verify; `subscription_created`/`subscription_updated`(active) → activate, `subscription_expired` → deactivate, `subscription_cancelled` → recorded (access kept until expiry); user id from `meta.custom_data.user_id`.
- `routers/billing.py`: `POST /api/v1/billing/checkout` (builds Payme base64 / Click query / Lemon Squeezy hosted-checkout URL via their API), `POST /cancel`, `GET /billing`.
- `main.py`: registered billing + the three webhook routers at bare `/webhooks/*`.
- Frontend: public `/pricing` (4 tiers, provider buttons), `(app)/billing` (tier/status/period + cancel), `PaywallModal` + `useTier()`, dashboard "Tarif" card, `/billing` added to middleware PROTECTED, `lib/api.ts` billing client + `getMe`.
- Tests: billing migration, billing service (tier map, default period, cancel), Payme webhook (auth, amount, perform→activate, unknown tx/method), Click webhook (invalid-sign no-write, prepare/complete happy path, wrong amount), Lemon Squeezy webhook (bad-sig→400 no activate, created→activate, expired→deactivate, cancelled→recorded).

## Problems encountered — Phase 7

1. **PayTechUz is unusable with our stack** — the blueprint/task mandate `paytechuz` for Payme + Click, but `paytechuz[fastapi]==0.3.51` pins `pydantic<2.0` (clashes with our locked Pydantic v2 + Pydantic AI), its FastAPI handlers need a **sync SQLAlchemy `Session` + an `account_model` ORM + their own `payments` table** (the app is 100% asyncpg + Supabase RLS, no ORM), and the webhook logic is gated behind a **paid `PAYTECH_LICENSE_API_KEY`**. This was the exact blocker flagged back in Phase 0.

2. **Stripe is not available in Uzbekistan** — the blueprint's international card rail is Stripe, but Stripe doesn't onboard UZ-based businesses, so it can't be the merchant of record here.

3. **No merchant sandbox creds** — Payme/Click/Lemon Squeezy live callbacks can't be exercised in this environment, same as every prior external-service phase.

## Solutions — Phase 7

1. Confirmed with the user, then implemented both protocols **natively** against asyncpg/Supabase — the blueprint documents the Payme JSON-RPC methods and the Click MD5 sign formulas exactly, so the native code is a faithful, license-free, fully-typed implementation that matches the rest of the codebase. Deviation documented in `TODO.md` per CLAUDE.md.

2. Replaced Stripe with **Lemon Squeezy** (a Merchant of Record that supports UZ sellers and handles international cards + tax) — same role, 1:1 swap, still three rails total. Removed the `stripe` dependency; Lemon Squeezy needs only `httpx` (checkout API) + `hmac` (webhook signature), both already present. Deviation documented in `TODO.md`.

3. Implemented the full integration boundary and unit-tested every acceptance criterion that doesn't need a live merchant (invalid Click sign → `error=-1` no write; Payme `-32504`/`-31001`; Lemon Squeezy bad `X-Signature` → 400; activate/deactivate paths). Documented the exact live criteria that remain unverified plus the webhook URLs and env vars to set in each cabinet.

## Phase 7 wrap-up
- `mypy` clean (69 files), 144/144 backend tests pass, web typecheck + build green (17/17 routes incl. public `/pricing` + `/billing`).
- Free-tier usage metering deferred to Phase 8 (PaywallModal + `useTier()` already in place).

## Next plan
- Phase 8: eval harness (`evals/`), Langfuse tracing + Sentry, free-tier usage metering to drive the paywall, demo polish.

## Time spent — Phase 7
- ~2h

## Demo video
- None this entry
